package com.baoli.pricer.mapper;

import org.apache.ibatis.annotations.Mapper;

/**
 * 仅用于承载公共 SQL 片段 (VersionFilter) 的 Mapper Namespace。
 * 不需要定义方法。
 */
@Mapper
public interface CommonSqlMapper {
    // empty
}
