<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="common-sql">
    <!-- 公共的 version 过滤 -->
    <sql id="VersionFilter">
        AND version_id = #{versionId}
    </sql>
</mapper>
