spring:
  servlet:
    multipart:
      max-file-size: 3GB
      max-request-size: 3GB

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://47.115.47.145:3306/baoli?useSSL=false&characterEncoding=UTF-8&serverTimezone=UTC&allowPublicKeyRetrieval=true
#    url: jdbc:mysql://192.168.81.141:3306/baoli?useSSL=false&characterEncoding=UTF-8&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: baoliuser
    password: "#ALiYun123"

  task:
    execution:
      pool:
        core-size: 8
        max-size: 32
        queue-capacity: 100

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: always

server:
  port: 8080
  address: 0.0.0.0
  tomcat:
    max-swallow-size: -1      # 禁用 Tomcat 的 swallow 限制
    max-http-form-post-size: 0
  max-http-request-header-size: 0

# MyBatis
mybatis:
  mapper-locations: classpath*:mapper/**/*.xml
  type-aliases-package: com.baoli.pricer.pojo
  configuration:
    map-underscore-to-camel-case: true

minio:
  endpoint: http://47.115.47.145:9000
#  endpoint: "192.168.81.141:9000"
  access-key: minioadmin
  secret-key: minioadmin
  bucket: baoliphotos

pagehelper:
  helper-dialect: mysql
  reasonable: true
  support-methods-arguments: true
  params: count=countSql
